# 큐시트 — {{N}}편 「{{제목}}」

> 대본 {{X.X}}분 · 슬라이드 **{{N}}장** · {{시리즈 위치}}
> 🔴 **세이프존 실측 {{YYYY-MM-DD}}** — 침범 {{N}}장 / 상단 잘림 {{N}}장 / 최대 하단 {{N}}px (기준 {{N}}px)
> 🔑 **이 편의 주인공은 {{무엇}}입니다.** {{왜 그것이 이 편을 성립시키는가 한 줄}}

---

## 🔴 촬영 전 준비 — {{준비물 이름}}

> 슬라이드에 값이 적혀 있어도 **실제 화면을 함께 보여주면 설득력이 다릅니다.**
> 아래를 **촬영 전날** 만들어 `_capture/` 폴더에 넣으십시오.
> 준비물이 없는 편이면 이 절을 통째로 지웁니다 — 빈 칸을 남기지 마십시오.

### ① {{준비물 1}}

{{그대로 복붙해서 실행할 수 있는 명령 또는 절차. 몇 초 걸리는지 함께 적는다}}

**기대 결과** — {{무엇이 나와야 정상인가}}

🔴 **다르게 나오면 촬영을 멈추고 알려주십시오.** {{무엇이 바뀐 것이고, 그러면 대본의 어느 전제가 무너지는가}}

> 📌 이미 측정해 둔 결과: `_capture/{{파일명}}` ({{측정일}} · {{요약}})

### ② {{준비물 2 — 캡처}}

{{어디서 무엇을 캡처하는가}} → `_capture/{{파일명}}.png` 로 저장.

### ③ {{준비물 3 — 재현이 흔들리는 것}}

**하는 법**
1. {{단계}}
2. {{단계}}
3. {{단계}}

**⚠️ 그날 결과가 다를 수 있습니다.**

| 그날 결과 | 어떻게 할까 |
|---|---|
| {{기대대로}} | ✅ 그대로 진행. 캡처해서 씁니다 |
| {{약함}} | 조건을 바꿔 재시도 — {{어떤 조건이 잘 나오는지}} |
| {{전제가 무너짐}} | 🔴 **대본 수정이 필요합니다.** 촬영 멈추고 알려주십시오 |

> 📌 {{측정일}} 기록: {{그날 실제로 나온 값}}

---

## 🔴 장비 점검 — 큐시트 만들 때 «실제로» 확인한다

촬영 당일에 없는 걸 발견하면 그날이 통째로 날아간다. **아래 넷은 전부 촬영 직전에 발견된 실사례다.**

- [ ] **웹캠** — 잡히나? 없으면 `F` 키(얼굴 없음 모드)로 갈지 **먼저 정한다**
- [ ] **마이크** — 잡히나? 모니터 스피커만 있고 입력이 없는 경우가 흔하다
- [ ] **모니터 해상도** — 🔴 슬라이드는 **1920×1080 고정**이다. 울트라와이드(2560×1080)에 전체화면하면
      좌우에 검은 띠가 생기고, 그대로 캡처하면 유튜브에서 **레터박스가 두 번** 겹친다.
      **1920×1080 모니터에 올린다**
- [ ] **OBS 출력 해상도** — 🔴 **모니터와 다른 칸이다.** 모니터를 1920×1080으로 맞춰도
      OBS 「출력(조정) 해상도」가 1280×720이면 **그대로 줄여 저장한다**
- [ ] **녹화 품질** — 「스트리밍과 동일」이면 스트리밍용 낮은 비트레이트가 쓰인다. 1080p는 **8Mbps** 권장
- [ ] **마이크 음량** — 말할 때 미터가 **-12 ~ -6dB**에서 놀아야 한다.
      작게 녹음된 것을 유튜브는 **키워주지 않는다**

```powershell
# 웹캠·마이크·모니터 한 번에 (Windows)
Add-Type -AssemblyName System.Windows.Forms
[System.Windows.Forms.Screen]::AllScreens | % { "$($_.DeviceName) $($_.Bounds.Width)x$($_.Bounds.Height) 주=$($_.Primary)" }
Get-PnpDevice -Class Camera,AudioEndpoint -Status OK | ? FriendlyName -match 'cam|Micro|마이크' | % FriendlyName
```

🔴 **위 세 줄(OBS 출력·품질·음량)은 눈으로 보지 말고 «잰다».** 「고쳤겠지」가 15분을 날린 원인이다.

```bash
node "<jedi-youtube-script 스킬 폴더>/scripts/check-recording.js" --obs                     # 촬영 직전
node "<jedi-youtube-script 스킬 폴더>/scripts/check-recording.js" --obs <10초테스트.mkv>     # 시험 녹화까지
```

🔴 **대본을 고쳤다면 슬라이드도 고쳤는지 잰다** — 프롬프터는 대본 파일이 아니라 슬라이드의 `data-script` 를 읽는다.

```bash
node "<jedi-youtube-script 스킬 폴더>/scripts/check-deck-sync.js" <대본.md> <슬라이드.html>
```

기대: **실패 0건**. 낭독 원고 동기화와 «실제 화면 캡처 장수» 를 함께 잰다.

⚠️ **OBS를 끄고 재라** — 켜 둔 채로는 바꾸기 «전» 값이 파일에 남아 있다. 기대: **실패 0건.**

---

## 🎬 촬영 셋업

| 항목 | 값 |
|---|---|
| 녹화 | OBS — 「화면 캡처 + 웹캠」 2소스를 겹쳐 **한 파일로** |
| 잡을 창 | **슬라이드 창만.** 🔴 프롬프터 창이 들어가면 대본이 방송된다 |
| 잡을 모니터 | **1920×1080 인 것.** 울트라와이드면 레터박스가 겹친다 |
| PiP | 정본 `production` 값 (우하단) · 얼굴 없이 찍으면 `F` 키 |
| 가이드 | `G` 키로 세이프존 확인 → **촬영 시작 전에 끈다** |
| 프롬퍼 | `P` 키 → 둘째 화면으로 · `T` 로 타이머 |

---

## 구간표

| # | 슬라이드 | 구간 | 예상 | 끊어 찍기 | 비고 |
|---|---|---|---|---|---|
| 1 | 1~{{N}} | ① 후크 (0~1분) | {{X.X}}분 | ✂️ 여기서 한 번 | 자기소개·로고 없음 |
| 2 | {{N}}~{{N}} | ② 결론 먼저 (1~3분) | {{X.X}}분 | | {{핵심 숫자}} |
| 3 | {{N}}~{{N}} | ③ 실전 (3~6분) | {{X.X}}분 | ✂️ | {{증거 화면 띄우는 지점}} |
| 4 | {{N}}~{{N}} | ④ 몰입 (6분~) | {{X.X}}분 | | **CTA는 여기서만** |

🔴 **끝나는 신호를 주지 않습니다** — "오늘은 여기까지" · "정리하자면" · "다음 영상에서" 금지.
마지막 장은 하던 말을 하다가 **뚝 끊습니다.**

---

## 촬영 순서

1. 조명·의상·배경 세팅 (몰아찍는 날 1회 — 이후 전 편이 자동으로 일관됩니다)
2. `_capture/` 준비물 확인 — **하나라도 비었으면 여기서 멈춥니다**
3. 슬라이드 전체화면(`F11`) → `G` 로 세이프존 확인 → **끄기**
4. 프롬프터(`P`)를 둘째 화면으로
5. OBS 소스 확인 — 슬라이드 창만 잡혔는지 **녹화 전에** 미리보기로 확인
6. 구간별로 녹화 시작·정지
7. 같은 셋업으로 **다음 편도 이어서** (몰아찍기)

---

## 촬영 후 — 여기서 재산정할 것

⚠️ 아래는 렌더로만 확인된 값입니다. 첫 촬영본을 보고 채워 주십시오.

- [ ] PiP 크기가 적절한가 (표정이 읽히나 / 슬라이드를 먹지 않나) → 정본 `production` 갱신
- [ ] 애니메이션 속도가 낭독과 맞나
- [ ] 장당 체류 시간이 충분한가 (슬라이드 장수 기준)
- [ ] 세이프존 여유 60px 미만이던 장이 실제로 가려졌나
